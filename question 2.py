# תוכנית המקבלת מחרוזת ומחזירה את התו האמצעי שלה. אם המחרוזת באורך זוגי מחזירה את שני
# התווים האמצעים.

x = input("הכנס מחרוזת\n")
b = len(x)
if b % 2 != 0:
    if b == 1:
        print(x)
    else:
        print(int(b // 2 + 1))
else:
    print(x[b // 2 - 1:b // 2 + 1])
